# Ollama host and port
OLLAMA_HOST=127.0.0.1
OLLAMA_PORT=11434

# Embedding and generation model names (installed in Ollama)
EMBED_MODEL=all-minilm
LLM_MODEL=llama3.2

# Chroma database persistence path
CHROMA_PERSIST_DIR=./chroma_db
